# Screenshots for submission

Place here:

1. **Grafana** – Dashboard showing latency, error rate, and trace link.
2. **Jaeger** – Trace view with spans for the Flask app.
3. **Logs** – Sample JSON log line containing `trace_id` and `span_id` (e.g. from CloudWatch or stdout).
4. **Alert** – Prometheus or Grafana alert firing (error rate or latency).

These demonstrate alert → trace → log correlation for root cause identification.
